{{if $member->member_id == 1}}
  html to display                         // A code for IPS4 to change shit for USER ID *1*
  {{endif}}

{member="name"} // shows the the member name uses \IPS\Member::loggedIn()->name
{member="id"} // shows the member id
